package labor;

import java.io.IOException;

import org.apache.ibatis.io.Resources;
import org.apache.ibatis.session.SqlSession;
import org.apache.ibatis.session.SqlSessionFactory;
import org.apache.ibatis.session.SqlSessionFactoryBuilder;

import com.oracle.labor.dao.ZjDwzpdjbMapper;

public class Test {
	
          public static void main(String []args) throws IOException{
        	    /*SqlSessionFactory factory=new SqlSessionFactoryBuilder().build(Resources.getResourceAsReader("applicationContext.xml"));
        	    	SqlSession session=factory.openSession();
        	    	
        	    	ZjDwzpdjbMapper dao=session.getMapper(ZjDwzpdjbMapper.class);
        	    	
        	    	System.out.println(dao.getAll_wgd().toString());*/
        	  
        	    	
          }
}
